PORT=3000
# Optional: configure SMTP to enable auto-email (feature stub in place)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_user
SMTP_PASS=your_password
SMTP_FROM="Poster Bot <no-reply@example.com>"
